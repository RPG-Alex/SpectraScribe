[package]
name = "burn-guide"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.20.1", features = ["fusion", "std", "train", "tui", "vision", "wgpu"], default-features = false}
